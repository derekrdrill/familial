import styled from 'styled-components';
import { Grid } from '@mui/material';
import tw from 'twin.macro';

export const BodyRoot = styled(Grid)(() => [tw`absolute`, tw`top-28`, tw`lg:top-16`, tw`z-0`]);
